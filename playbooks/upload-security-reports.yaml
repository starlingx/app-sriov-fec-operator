- hosts: all
  tasks:
    - name: Upload security report
      synchronize:
        src: "{{ zuul.project.src_dir }}/UTCoverage/"
        dest: "{{ zuul.executor.log_root }}/security/"
        mode: pull
      failed_when: false
    - name: Return security report artifact URL
      zuul_return:
        data:
          zuul:
            artifacts:
              - name: "Security Report"
                url: "security/security_report.txt"
